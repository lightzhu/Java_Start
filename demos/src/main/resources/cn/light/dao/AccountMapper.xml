<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.light.dao.AccountDao">
    <!--id属性为对应DAO对应方法的名字-->
    <select id="findAll" resultType="Account">
        select * from account
    </select>
</mapper>